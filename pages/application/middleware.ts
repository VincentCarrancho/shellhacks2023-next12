export default function middleware() {
  // the user must already be logged in. If not, it will redirect to the home page

  let userIsSignedIn = true;
  if (!userIsSignedIn) {
    // If the user isnt signed in
    // redirect to the sign in page
  } else {
    // since the user is signed in we want to fetch the data right here
  }
}
